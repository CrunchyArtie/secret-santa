import { SnowDirective } from './snow.directive';

describe('SnowDirective', () => {
  it('should create an instance', () => {
    // const directive = new SnowDirective();
    // expect(directive).toBeTruthy();
  });
});
